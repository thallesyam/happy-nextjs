import { icon } from 'leaflet'
import iconUrl from '../../public/images/happy-icon.svg'

export const mapIcon = icon({
  iconUrl,
  iconSize: [58, 68],
  iconAnchor: [29, 68],
  popupAnchor: [170, 2],
})
