import styled from 'styled-components'

export const Container = styled.div`
  strong,
  p {
    color: var(--white);
    font-size: 1.25rem;
  }

  > strong {
    font-weight: 800;
  }

  > p {
    font-weight: 400;
  }
`
